{
  "name": "seattledogparkdata-build",
  "private": true,
  "version": "0.0.0",
  "description": "Build tooling for seattledogparkdata.com. The site itself is static HTML/CSS/JS; this package exists only to generate the combined PDF report.",
  "type": "module",
  "scripts": {
    "build:pdf": "node scripts/build-pdf.mjs"
  },
  "devDependencies": {
    "pdf-lib": "^1.17.1",
    "puppeteer": "^23.10.4"
  }
}
