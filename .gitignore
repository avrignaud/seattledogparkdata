# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Editor
.vscode/
.idea/
*.swp
*~

# Python (in case any analysis scripts get added)
__pycache__/
*.py[cod]
venv/
.venv/
env/

# Node (in case any build tooling gets added)
node_modules/
npm-debug.log*
yarn-debug.log*

# OS
Thumbs.db

# Local scratch
scratch/
tmp/
*.local.*

# Local agent/tool config (not source-controlled)
.claude/
.railroad/

# osmnx cache — large OSM tile blobs regenerated on demand
cache/

# Agent/context scratch files
AGENTS.md

# docs/report.html — paused WIP
docs/report.html

# TPL ParkServe national shapefile — too big for the repo; we keep only the
# Seattle-filtered GeoJSON derived from it in data/tpl-parkserve/.
data/ParkScore_2025_DataDownloads_shapefiles*/

